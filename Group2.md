# OpenRAM SRAM Integration in Wildcat (Caravel Tapeout Project)

## Introduction

This project focuses on integrating a dense SRAM macro generated with OpenRAM into the Wildcat RISC-V processor, targeting a full ASIC flow using the Caravel framework and the Sky130 PDK.

The initial motivation was to replace the existing `SyncReadMem`-based memory used in the Wildcat design. While convenient at the RTL level, such memories are synthesized into flip-flops and multiplexers, leading to extremely inefficient area usage and unrealistic implementations for ASIC tapeout.

The goal is therefore to:
- Replace this memory with a compact SRAM macro generated by OpenRAM
- Integrate it into the Wildcat processor
- Harden the design using the LibreLane/OpenLane flow
- Prepare a design suitable for real tapeout

While the project initially appeared straightforward, integrating OpenRAM turned out to be one of the most challenging parts of the entire flow. Significant effort was required to handle memory generation, macro integration, and physical design constraints. We ended up integrating a memory already generated and tested, available in the PDK.

---

## Design

### Overview

The design consists of:
- The Wildcat RISC-V processor
- A custom SRAM block generated using OpenRAM
- Integration within the Caravel `user_project_wrapper`
---

### SRAM Architecture

- Current prototype: **256 × 32-bit SRAM**
- Available in the PDK
- Intended to be replaced by a larger macro in the final version

The SRAM is integrated as a **hard macro (blackbox)**:
- The macro is generated externally (GDS + LEF + Liberty)
- The processor interacts with it through a defined memory interface
- The macro is instantiated at the top level during physical design

---

### Integration Strategy

The integration required several steps:

- Removal of `SyncReadMem` from the Wildcat design
- Adaptation of the memory interface to match SRAM behavior
- Instantiation of the SRAM macro in the design hierarchy
- Configuration of the OpenLane/LibreLane flow to support external macros
- Integration into the Caravel `user_project_wrapper`

---

### Practical Challenges

Although conceptually simple, the integration exposed several non-trivial issues:

- **OpenRAM generation complexity**  
  Generating a working SRAM macro required careful configuration and debugging.

- **LEF / GDS consistency**  
  Ensuring that the generated macro views (LEF, GDS, Liberty) were consistent and correctly interpreted by the flow.

- **DRC violations**  
  The generated memory introduced design rule violations that required investigation and mitigation.

- **Macro integration in OpenLane**  
  Hard macro integration is significantly more complex than standard-cell flows, requiring:
  - Correct placement constraints
  - Proper abstraction handling
  - Flow configuration adjustments

- **Flow instability**  
  Some stages of the flow were more sensitive to the macro and required iterative debugging.

Overall, integrating OpenRAM proved to be the **main bottleneck of the project**, both in terms of time and complexity.

---

## OpenRAM generation

[OpenRAM](https://github.com/VLSIDA/OpenRAM/tree/stable) is a Python framework to generate SRAM. It creates the layout, netlists, timing and power models, placement and routing models.

Every SRAM is an array of bitcells.
Here we can see the different bitcells for the Sky130 technology, and the comparison with a D flip flop (comes from the OpenRAM documentation).

## Thin SRAM Bitcells (130nm)
| <img height="158" src="assets/thin_single.png"> | <img height="158" src="assets/thin_dual.png"> | <img height="158" src="assets/thin_single_straps.png">   | <img height="197" src="assets/thin_dual_straps.png"> |
| :--------------------------------------------------------: | :------------------------------------------------------: | :-----------------------------------------------------------------: | :-------------------------------------------------------------: |
| Single Port 1.2um x 1.58um                                 | Dual Port 2.40um x 1.58um                                | Single Port (w/ straps & taps) 2.49um x 1.58um                      | Dual Port (w/ straps & taps) 3.12um x 1.97um                    |

| <img height="707" src="assets/dff_reference.png"> |
| :----------------------------------------------------------: |
| DFF (for reference) 5.83um x 7.07 um                         |

In the array, bitcells are placed in quadruplets, in a symmetric way. One columns per port is also placed on eac

Initially, we were generating on our laptops, using WSL, as OpenRAM works only under Linux. We couldn't make the script work, even when following the basic tutorial.
Most errors were sadist because they happened at the very end of the generation (◡︵◡). The errors came from two things :
  - conflict between our skywater PDK in ciel, installed for Librelane, with the one from OpenRAM
  - OpenRAM not being perfectly compatible with new versions of the PDK

After some tweaking we succeeded to generate an SRAM, but it was extremely slow. 
We therefore switched to a Colab, which was way faster. It made it easier to try configurations.

The generation would still fail sometimes due to :
- too complex SRAM, that OpenRAM has trouble routing
- problems in symmetry
- miscellaneous fails

The SRAM we aimed to generate was a 32 bits per words, 256 words

---

## Design Implementation

The integration of the OpenRAM SRAM required modifications across multiple parts of the design, including RTL, memory abstraction, and physical configuration.

### OpenRAM Macro Files

The SRAM macro generated with OpenRAM includes the following views:

- `.v` — behavioral model of the SRAM
- `.lef` — abstract physical layout for place & route
- `.lib` — timing model for static timing analysis
- `.gds` — final physical layout

These files are required for proper integration into the ASIC flow and are referenced in the OpenLane configuration.

---

### Modified Files

#### `CpuTop.scala`

The original Wildcat design used a `SyncReadMem` for data memory, which is not suitable for ASIC implementation.

This file was modified to:
- Remove the `SyncReadMem`-based memory
- Interface the CPU with an external SRAM macro
- Adapt control signals for read/write operations

This modification is central to enabling a physically realizable memory.

---

#### `config.json`

The OpenLane configuration was updated to support the SRAM macro:

- Inclusion of SRAM `.lef`, `.gds`, and `.lib` files
- Configuration for hard macro integration
- Adjustment of flow parameters for macro handling

---

### Created Files

#### OpenRAM BlackBox

- `OpenRam256x32BlackBox.scala`

This file:
- Defines the interface between the CPU and the SRAM
- Allows synthesis without exposing internal SRAM implementation
- Matches the generated OpenRAM macro interface

---

#### SRAM Integration Module

- `OpenRamScratchPadMem.scala`

This module:
- Replaces the original `ScratchPadMem.scala`
- Connects the CPU to the OpenRAM SRAM
- Implements memory access logic compatible with the macro

---

### Summary of Changes

The following transformations were performed:

- `SyncReadMem` → OpenRAM SRAM macro
- Internal behavioral memory → External hard macro
- FPGA-oriented design → ASIC-compatible implementation

This required coordinated changes across:
- RTL (Chisel modules)
- Memory abstraction
- OpenLane configuration
- Physical macro integration

## Testing and Verification

To ensure the reliability of the integrated OpenRAM macro, we developed a comprehensive functional verification environment. This was necessary because the default behavioral models provided by memory generators often require adaptation for open-source simulation tools like Icarus Verilog.

### Simulation Environment

The verification flow was built using the following stack:
* **Simulator**: Icarus Verilog (installed via Homebrew).
* **Waveform Viewer**: WaveTrace (VS Code extension), utilized due to compatibility issues with GTKWave on modern macOS versions.
* **Hardware Description**: A custom Verilog testbench (`sram_test.v`) designed to exercise the `sky130_sram_1kbyte_1rw1r_32x256_8.v` module.

### Detailed Test Suite

We performed three primary categories of tests to validate the memory's behavior:

* **Full Memory Write/Read**: This test iterates through every single address in the 256 × 32-bit range. It writes a unique, identifiable data pattern to each location and subsequently reads it back to verify that every cell in the array correctly retains information without cross-talk or addressing errors.
* **Write Mask Validation**: We tested the SRAM's ability to perform masked writes. This ensures that the `wmask0` signals correctly allow the processor to update specific bytes of a 32-bit word while leaving the other bytes unchanged, which is critical for supporting various RISC-V store instructions (sb, sh, sw).
* **Simultaneous Access and Latency**: This test verifies the timing of the synchronous interface. We simulated back-to-back read and write operations to confirm that the Chip Select (`csb0`) and Write Enable (`web0`) signals operate correctly under maximum throughput conditions without violating the internal timing requirements of the macro.

### Waveform Analysis

The simulation results confirm that the SRAM functions exactly as intended for a synchronous hard macro.

!(Wave_form.png)

**Key Observations from the Waveform:**
* **Read Latency**: The waveform clearly demonstrates the **1-cycle read latency**. As seen in the signals, when `addr0` transitions to `3D`, the data bus `dout0` provides the value previously stored at `3C` (`3C3C3C3C`). The data for address `3D` appears on the subsequent rising edge of `clk0`.
* **Synchronous Alignment**: All transitions on the data output bus (`dout0`) are strictly aligned with the rising edge of the clock, confirming stable synchronous behavior suitable for the RISC-V pipeline.
* **Control Signal Integrity**: The Chip Select (`csb0`) and Write Enable (`web0`) signals show correct timing relative to the address transitions, ensuring no race conditions occur during high-speed access.
* **Data Consistency**: the data appearing on `dout0` (`3B3B3B3B`, `3C3C3C3C`, `3D3D3D3D`, etc.) perfectly matches the expected incremental test patterns, proving the integrity of the memory array.

### Integration Lessons Learned

The testing phase provided several insights into the OpenRAM flow:
1.  **Model Compatibility**: Original foundry models often include `specify` blocks and non-standard delays. We replaced these with a synthesizable `always @(posedge clk)` model to enable simulation in open-source environments.
2.  **Timing Accuracy**: The testbench initially failed because it attempted to sample data on the same cycle as the request. Adjusting the logic to account for the synchronous delay was essential for a passing result.

### How to Run the Simulation

To reproduce these results, follow these steps in the `sram_test` directory:

1.  **Install Tools**: `brew install icarus-verilog`.
2.  **Compile**: `iverilog -o sram_test.vvp sram_test.v sky130_sram_1kbyte_1rw1r_32x256_8.v`.
3.  **Execute**: `vvp sram_test.vvp`.
4.  **View**: Open the resulting `.vcd` file in VS Code using the **WaveTrace** extension.
 
---

## Final Remarks

This project highlights an important reality of ASIC design:

> Integrating a memory macro is not a trivial task, even when using open-source tools.

What initially seemed like a simple replacement of a memory block turned into a complex integration problem involving:
- Toolchain limitations
- Physical design constraints
- Non-trivial debugging across multiple abstraction levels

This experience provided valuable insight into the challenges of real-world chip design and tapeout preparation.

---

## Authors

- Célien Abbet  
- Amaury Chevoir  
- Aldric Rüedi 
